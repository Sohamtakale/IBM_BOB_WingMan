# Test dependencies for WingMan backend test suite

# Core testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# HTTP testing
httpx>=0.27.0

# Already in main requirements but listed for completeness
fastapi>=0.115.0
pydantic>=2.0.0

# Note: Main application dependencies from backend/requirements.txt
# should also be installed for tests to run properly